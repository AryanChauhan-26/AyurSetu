services:
  - type: web
    name: ayursetu-fullstack
    env: node
    plan: free
    buildCommand: npm install && npm --prefix server install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
